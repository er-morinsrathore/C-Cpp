#include<stdio.h>

void main(){
    char playlist[] = "Mood"; //Can use single or double inverted commas during initialisation.
    int songs = 10;
    float avgDuration = 3.70;

    printf("Fav playlist name is:  %s", playlist);
    printf("\tTotal number of songs:  %d", songs);
    printf("\tAverage duration of songs:  %f", avgDuration);
}